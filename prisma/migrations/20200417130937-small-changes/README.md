# Migration `20200417130937-small-changes`

This migration has been generated by Alex Karavil at 4/17/2020, 1:09:37 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Project" ADD COLUMN "completed" boolean  NOT NULL DEFAULT false,
ADD COLUMN "description" text   ,
ADD COLUMN "name" text  NOT NULL ;

ALTER TABLE "public"."Resource" ADD COLUMN "description" text   ;

ALTER TABLE "public"."Task" DROP COLUMN "name",
ADD COLUMN "completed" boolean  NOT NULL DEFAULT false,
ADD COLUMN "description" text  NOT NULL ,
ADD COLUMN "notes" text   ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200417130453-init..20200417130937-small-changes
--- datamodel.dml
+++ datamodel.dml
@@ -1,27 +1,33 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
 }
 model Project {
-  id        Int        @default(autoincrement()) @id
-  tasks     Task[]
-  resources Resource[] @relation(references: [id])
+  id          Int        @default(autoincrement()) @id
+  name        String
+  description String?
+  tasks       Task[]
+  resources   Resource[] @relation(references: [id])
+  completed   Boolean    @default(false)
 }
 model Task {
-  id        Int     @default(autoincrement()) @id
-  name      String
-  project   Project @relation(fields: [projectId], references: [id])
-  projectId Int
+  id          Int     @default(autoincrement()) @id
+  description String
+  notes       String?
+  project     Project @relation(fields: [projectId], references: [id])
+  projectId   Int
+  completed   Boolean @default(false)
 }
 model Resource {
-  id       Int       @default(autoincrement()) @id
-  name     String
-  projects Project[] @relation(references: [id])
+  id          Int       @default(autoincrement()) @id
+  name        String
+  description String?
+  projects    Project[] @relation(references: [id])
 }
```



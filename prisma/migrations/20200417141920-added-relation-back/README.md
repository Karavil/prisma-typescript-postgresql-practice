# Migration `20200417141920-added-relation-back`

This migration has been generated by Alex Karavil at 4/17/2020, 2:19:20 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."_ProjectToResource" (
    "A" integer  NOT NULL ,
    "B" integer  NOT NULL 
) 

ALTER TABLE "public"."Resource" DROP CONSTRAINT IF EXiSTS "Resource_projectId_fkey",
DROP COLUMN "projectId";

CREATE UNIQUE INDEX "_ProjectToResource_AB_unique" ON "public"."_ProjectToResource"("A","B")

CREATE  INDEX "_ProjectToResource_B_index" ON "public"."_ProjectToResource"("B")

ALTER TABLE "public"."_ProjectToResource" ADD FOREIGN KEY ("A")REFERENCES "public"."Project"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_ProjectToResource" ADD FOREIGN KEY ("B")REFERENCES "public"."Resource"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200417141516-removed-resource-to-project-relation..20200417141920-added-relation-back
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -11,9 +11,9 @@
   id          Int        @default(autoincrement()) @id
   name        String
   description String?
   tasks       Task[]
-  resources   Resource[]
+  resources   Resource[] @relation(references: [id])
   completed   Boolean    @default(false)
 }
 model Task {
@@ -25,8 +25,9 @@
   completed   Boolean @default(false)
 }
 model Resource {
-  id          Int     @default(autoincrement()) @id
+  id          Int       @default(autoincrement()) @id
   name        String
   description String?
+  projects    Project[] @relation(references: [id])
 }
```


